import React from 'react';
import TestActivityConnection from '../components/CaseManagementPage/TestActivityConnection';
import PageContainer from '../components/shared/PageContainer';

const TestActivity: React.FC = () => {
  return (
    <PageContainer>
      <TestActivityConnection />
    </PageContainer>
  );
};

export default TestActivity; 